# @dodamind/preset-tsconfig

tsconfig.json preset for dodamind projects.

## Install

```sh
npm install @dodamind/preset-tsconfig --save-dev
```
## Usage
### tsconfig.json
```json
{
  "extends": ["@dodamind/preset-tsconfig"],
  // ...
}
```
